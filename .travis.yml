sudo: required
services:
  - docker

before_install:
  - docker build -t krishnadasck/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true krishnadasck/react-test npm test -- --coverage

after_success:
  - docker build -t krishnadasck/multi-client ./client
  - docker build -t krishnadasck/multi-nginx ./nginx
  - docker build -t krishnadasck/multi-server ./server
  - docker build -t krishnadasck/multi-worker ./worker
  # login to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push krishnadasck/multi-client
  - docker push krishnadasck/multi-nginx
  - docker push krishnadasck/multi-server
  - docker push krishnadasck/multi-worker